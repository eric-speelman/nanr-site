# HTML
# Archive your static HTML project and save it with the build record.
# Add steps that build, run tests, deploy, and more:
# https://aka.ms/yaml

trigger:
- test

pool:
  vmImage: 'master-latest'

steps:
- task: DeleteFiles@1
  inputs:
    SourceFolder: '.git'
    Contents: '/*'
    RemoveSourceFolder: true
    
- task: HugoTask@1
  inputs:
    source: 
    destination: '$(System.DefaultWorkingDirectory)/nanr-site'